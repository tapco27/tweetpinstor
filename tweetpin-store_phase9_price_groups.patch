diff --git a/app/Http/Controllers/Api/V1/Admin/AdminProductPriceController.php b/app/Http/Controllers/Api/V1/Admin/AdminProductPriceController.php
index 26f74ab..fa6b4f7 100644
--- a/app/Http/Controllers/Api/V1/Admin/AdminProductPriceController.php
+++ b/app/Http/Controllers/Api/V1/Admin/AdminProductPriceController.php
@@ -14,6 +14,7 @@ public function index(Request $r)
 
         if ($r->filled('currency')) $q->where('currency', strtoupper($r->currency));
         if ($r->filled('product_id')) $q->where('product_id', (int)$r->product_id);
+        if ($r->filled('price_group_id')) $q->where('price_group_id', (int)$r->price_group_id);
 
         return $q->orderByDesc('id')->paginate(50);
     }
@@ -27,6 +28,7 @@ public function store(Request $r)
     {
         $data = $r->validate([
             'product_id' => ['required','integer','exists:products,id'],
+            'price_group_id' => ['nullable','integer','exists:price_groups,id'],
             'currency' => ['required','in:TRY,SYP'],
             'minor_unit' => ['required','integer','in:0,2'],
             'unit_price_minor' => ['nullable','integer','min:0'],
@@ -35,6 +37,10 @@ public function store(Request $r)
             'is_active' => ['nullable','boolean'],
         ]);
 
+        if (empty($data['price_group_id'])) {
+            $data['price_group_id'] = 1;
+        }
+
         return ProductPrice::create($data);
     }
 
@@ -43,6 +49,7 @@ public function update(Request $r, $id)
         $pp = ProductPrice::findOrFail($id);
 
         $data = $r->validate([
+            'price_group_id' => ['sometimes','integer','exists:price_groups,id'],
             'currency' => ['sometimes','in:TRY,SYP'],
             'minor_unit' => ['sometimes','integer','in:0,2'],
             'unit_price_minor' => ['sometimes','nullable','integer','min:0'],
diff --git a/app/Http/Controllers/Api/V1/CatalogController.php b/app/Http/Controllers/Api/V1/CatalogController.php
index 1f107ca..6dae111 100644
--- a/app/Http/Controllers/Api/V1/CatalogController.php
+++ b/app/Http/Controllers/Api/V1/CatalogController.php
@@ -18,6 +18,8 @@ class CatalogController extends Controller
     public function home()
     {
         $currency = app('user_currency');
+        $priceGroupId = app()->bound('price_group_id') ? (int) app('price_group_id') : 1;
+        if ($priceGroupId <= 0) $priceGroupId = 1;
 
         $categories = Category::query()
             ->where('is_active', true)
@@ -44,10 +46,10 @@ public function home()
 
         // priceFor حسب العملة
         $featuredQ->with([
-            'priceFor' => fn ($x) => $x->where('currency', $currency)->where('is_active', true),
+            'priceFor' => fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true),
             'priceFor.packages' => fn ($x) => $x->where('is_active', true)->orderBy('sort_order'),
             'priceFor.packages.productPrice',
-        ])->whereHas('prices', fn ($x) => $x->where('currency', $currency)->where('is_active', true));
+        ])->whereHas('prices', fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true));
 
         $featured = $featuredQ->get();
 
@@ -88,6 +90,8 @@ public function banners()
     public function products()
     {
         $currency = app('user_currency');
+        $priceGroupId = app()->bound('price_group_id') ? (int) app('price_group_id') : 1;
+        if ($priceGroupId <= 0) $priceGroupId = 1;
 
         $limit = (int) request('limit', 20);
         $limit = $limit > 0 ? min($limit, 100) : 20;
@@ -116,10 +120,10 @@ public function products()
 
         // user: سعر واحد حسب عملته + packages
         $q->with([
-            'priceFor' => fn ($x) => $x->where('currency', $currency)->where('is_active', true),
+            'priceFor' => fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true),
             'priceFor.packages' => fn ($x) => $x->where('is_active', true)->orderBy('sort_order'),
             'priceFor.packages.productPrice',
-        ])->whereHas('prices', fn ($x) => $x->where('currency', $currency)->where('is_active', true));
+        ])->whereHas('prices', fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true));
 
         $p = $q->paginate($limit);
 
@@ -132,6 +136,8 @@ public function products()
     public function featured()
     {
         $currency = app('user_currency');
+        $priceGroupId = app()->bound('price_group_id') ? (int) app('price_group_id') : 1;
+        if ($priceGroupId <= 0) $priceGroupId = 1;
 
         $limit = (int) request('limit', 20);
         $limit = $limit > 0 ? min($limit, 50) : 20;
@@ -143,10 +149,10 @@ public function featured()
             ->limit($limit);
 
         $q->with([
-            'priceFor' => fn ($x) => $x->where('currency', $currency)->where('is_active', true),
+            'priceFor' => fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true),
             'priceFor.packages' => fn ($x) => $x->where('is_active', true)->orderBy('sort_order'),
             'priceFor.packages.productPrice',
-        ])->whereHas('prices', fn ($x) => $x->where('currency', $currency)->where('is_active', true));
+        ])->whereHas('prices', fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true));
 
         return $this->ok(ProductResource::collection($q->get()));
     }
@@ -154,12 +160,14 @@ public function featured()
     public function product($id)
     {
         $currency = app('user_currency');
+        $priceGroupId = app()->bound('price_group_id') ? (int) app('price_group_id') : 1;
+        if ($priceGroupId <= 0) $priceGroupId = 1;
 
         $product = Product::query()
             ->where('id', $id)
             ->where('is_active', true)
             ->with([
-                'priceFor' => fn ($x) => $x->where('currency', $currency)->where('is_active', true),
+                'priceFor' => fn ($x) => $x->where('currency', $currency)->where('price_group_id', $priceGroupId)->where('is_active', true),
                 'priceFor.packages' => fn ($x) => $x->where('is_active', true)->orderBy('sort_order'),
                 'priceFor.packages.productPrice',
             ])
diff --git a/app/Http/Controllers/Api/V1/OrderController.php b/app/Http/Controllers/Api/V1/OrderController.php
index 6cf256c..d310e67 100644
--- a/app/Http/Controllers/Api/V1/OrderController.php
+++ b/app/Http/Controllers/Api/V1/OrderController.php
@@ -94,6 +94,8 @@ public function store(CreateOrderRequest $request)
     {
         $user = auth('api')->user();
         $currency = app('user_currency');
+        $priceGroupId = app()->bound('price_group_id') ? (int) app('price_group_id') : 1;
+        if ($priceGroupId <= 0) $priceGroupId = 1;
 
         if (!$currency) {
             return $this->fail('Currency not set', 422);
@@ -121,6 +123,7 @@ public function store(CreateOrderRequest $request)
         $price = ProductPrice::query()
             ->where('product_id', $product->id)
             ->where('currency', $currency)
+            ->where('price_group_id', $priceGroupId)
             ->where('is_active', true)
             ->firstOrFail();
 
diff --git a/app/Http/Requests/Api/V1/CreateOrderRequest.php b/app/Http/Requests/Api/V1/CreateOrderRequest.php
index 0fd78cc..e1d72c8 100644
--- a/app/Http/Requests/Api/V1/CreateOrderRequest.php
+++ b/app/Http/Requests/Api/V1/CreateOrderRequest.php
@@ -52,10 +52,13 @@ public function withValidator(Validator $validator): void
         } else {
           // Validate min/max quantity using active price for current currency (avoid 500 from PricingService)
           $currency = app()->bound('user_currency') ? app('user_currency') : null;
+          $priceGroupId = app()->bound('price_group_id') ? (int) app('price_group_id') : 1;
+          if ($priceGroupId <= 0) $priceGroupId = 1;
           if ($currency) {
             $price = ProductPrice::query()
               ->where('product_id', $product->id)
               ->where('currency', $currency)
+              ->where('price_group_id', $priceGroupId)
               ->where('is_active', true)
               ->first();
 
diff --git a/app/Models/Product.php b/app/Models/Product.php
index 1c3bf19..e8d1286 100644
--- a/app/Models/Product.php
+++ b/app/Models/Product.php
@@ -51,8 +51,17 @@ public function priceFor(?string $currency = null)
       $currency ?? (app()->bound('user_currency') ? app('user_currency') : 'TRY')
     );
 
+    $priceGroupId = 1;
+    if (app()->bound('price_group_id')) {
+      $priceGroupId = (int) app('price_group_id');
+    }
+    if ($priceGroupId <= 0) {
+      $priceGroupId = 1;
+    }
+
     return $this->hasOne(ProductPrice::class, 'product_id')
       ->where('currency', $currency)
+      ->where('price_group_id', $priceGroupId)
       ->where('is_active', true);
   }
 }
diff --git a/app/Models/ProductPrice.php b/app/Models/ProductPrice.php
index caa393b..b1b9e96 100644
--- a/app/Models/ProductPrice.php
+++ b/app/Models/ProductPrice.php
@@ -8,6 +8,11 @@ class ProductPrice extends Model
 {
   protected $guarded = [];
 
+  protected $casts = [
+    'is_active' => 'boolean',
+  ];
+
   public function product(){ return $this->belongsTo(Product::class); }
+  public function priceGroup(){ return $this->belongsTo(PriceGroup::class, 'price_group_id'); }
   public function packages(){ return $this->hasMany(ProductPackage::class); }
 }
diff --git a/bootstrap/app.php b/bootstrap/app.php
index 800438a..ebdbe00 100644
--- a/bootstrap/app.php
+++ b/bootstrap/app.php
@@ -23,6 +23,7 @@
         $middleware->alias([
             'user.currency' => \App\Http\Middleware\EnforceUserCurrency::class,
             'currency.resolve' => \App\Http\Middleware\ResolveCurrency::class,
+            'price.group' => \App\Http\Middleware\ResolvePriceGroup::class,
             'admin' => \App\Http\Middleware\RequireAdmin::class,
         ]);
 
diff --git a/routes/api.php b/routes/api.php
index ac2ee61..5a8ae46 100644
--- a/routes/api.php
+++ b/routes/api.php
@@ -20,6 +20,7 @@
 use App\Http\Controllers\Api\V1\Admin\AdminBannerController;
 use App\Http\Controllers\Api\V1\Admin\AdminDigitalPinController;
 use App\Http\Controllers\Api\V1\Admin\AdminProviderIntegrationController;
+use App\Http\Controllers\Api\V1\Admin\AdminPriceGroupController;
 use App\Http\Controllers\Api\V1\Admin\AdminTweetPinController;
 use App\Http\Controllers\Api\V1\Admin\AdminTweetPinMappingController;
 
@@ -126,13 +127,20 @@
         // Phase 7: Tweet-Pin mapping helper
         Route::put('/products/{id}/tweetpin/mapping', [AdminTweetPinMappingController::class, 'update'])->whereNumber('id');
 
+        // Pricing Tiers (Price Groups)
+        Route::get('/price-groups', [AdminPriceGroupController::class, 'index']);
+        Route::post('/price-groups', [AdminPriceGroupController::class, 'store']);
+        Route::get('/price-groups/{id}', [AdminPriceGroupController::class, 'show'])->whereNumber('id');
+        Route::put('/price-groups/{id}', [AdminPriceGroupController::class, 'update'])->whereNumber('id');
+        Route::delete('/price-groups/{id}', [AdminPriceGroupController::class, 'destroy'])->whereNumber('id');
+
         // Product Catalog helpers
         Route::put('/products/order', [AdminProductController::class, 'updateOrder']);
         Route::put('/products/bulk-provider', [AdminProductController::class, 'bulkProvider']);
     });
 
     // Public Catalog (عملة افتراضية TRY + يدعم قراءة عملة المستخدم إذا أرسل Bearer token)
-    Route::middleware(['currency.resolve'])->group(function () {
+    Route::middleware(['currency.resolve', 'price.group'])->group(function () {
 
         // Home (endpoint واحد ممتاز للفلاتر)
         Route::get('/home', [CatalogController::class, 'home']);
@@ -156,7 +164,7 @@
         Route::middleware('throttle:auth')->post('/auth/refresh', [AuthController::class, 'refresh']);
 
         // كل ما تحتها يحتاج user.currency (للشراء/المدفوعات/المحفظة)
-        Route::middleware(['user.currency'])->group(function () {
+        Route::middleware(['user.currency', 'price.group'])->group(function () {
 
             // Payment Methods (User)
             Route::get('/payment-methods', [PaymentMethodController::class, 'index']);
